= erb :_job_info, :locals => {:job => @job, :type => :scheduled}
%form.form-horizontal{:action => "#{frontkiq_path}scheduled/#{job_params(@job, @job.score)}", :method => "post"}
  = csrf_tag
  %a.btn.btn-default{:href => "#{frontkiq_path}scheduled"}= t('GoBack')
  %input.btn.btn-primary{:name => "add_to_queue", :type => "submit", :value => "#{t('AddToQueue')}"}/
  %input.btn.btn-danger{:name => "delete", :type => "submit", :value => "#{t('Delete')}"}/
